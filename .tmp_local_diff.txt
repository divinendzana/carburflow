diff --git a/dashboard/rapport_views.py b/dashboard/rapport_views.py
index bcaff99..49dc0fa 100644
--- a/dashboard/rapport_views.py
+++ b/dashboard/rapport_views.py
@@ -191,6 +191,37 @@ class SoumissionsAPIView(APIView):
         return Response(items)
 
 
+class RapportDeleteAPIView(APIView):
+    """Suppression d'un rapport — réservée aux responsables.
+
+    Respecte le modèle Grinnel : LigneRapport.rapport est en CASCADE,
+    donc les lignes liées sont retirées avec le rapport (pas de migration).
+    """
+    permission_classes = [IsAuthenticated]
+
+    @extend_schema(tags=['Rapports'], summary='Supprimer un rapport (admin)')
+    def delete(self, request, rapport_id):
+        if not user_is_admin(request.user):
+            return Response(
+                {'detail': 'Seul un responsable peut supprimer un rapport.'},
+                status=status.HTTP_403_FORBIDDEN,
+            )
+        rapport = get_object_or_404(Rapport, pk=rapport_id)
+        rapport_pk = rapport.id
+        lignes = rapport.lignes.count()
+        rapport.delete()
+        return Response(
+            {
+                'detail': (
+                    f'Le rapport n°{rapport_pk} a été supprimé '
+                    f'({lignes} ligne(s) associée(s) retirée(s)).'
+                ),
+                'id': rapport_pk,
+            },
+            status=status.HTTP_200_OK,
+        )
+
+
 class RapportExportAPIView(APIView):
     """Téléchargement d'un rapport importé (admin: tous ; opérateur: les siens)."""
     permission_classes = [IsAuthenticated]
diff --git a/dashboard/urls.py b/dashboard/urls.py
index 62acf33..3d30342 100644
--- a/dashboard/urls.py
+++ b/dashboard/urls.py
@@ -19,6 +19,7 @@ from dashboard.rapport_views import (
     NormeCsvAPIView,
     NormeMetaAPIView,
     NormeXlsxAPIView,
+    RapportDeleteAPIView,
     RapportExportAPIView,
     RapportUploadAPIView,
     SoumissionsAPIView,
@@ -64,6 +65,11 @@ urlpatterns = [
         RapportExportAPIView.as_view(),
         name='api-rapport-export',
     ),
+    path(
+        'rapports/<int:rapport_id>/delete',
+        RapportDeleteAPIView.as_view(),
+        name='api-rapport-delete',
+    ),
 
     path('schema/', SpectacularAPIView.as_view(), name='schema'),
     path('docs/', SpectacularSwaggerView.as_view(url_name='schema'), name='swagger-ui'),
diff --git a/dashboard/views.py b/dashboard/views.py
index 4abdc2f..33d9722 100644
--- a/dashboard/views.py
+++ b/dashboard/views.py
@@ -44,6 +44,19 @@ class RapportViewSet(viewsets.ModelViewSet):
     queryset = Rapport.objects.all()
     serializer_class = RapportSerializer
 
+    def destroy(self, request, *args, **kwargs):
+        """Suppression réservée aux responsables (les lignes partent en CASCADE)."""
+        from rest_framework import status
+        from rest_framework.response import Response
+        from dashboard.permissions import user_is_admin
+
+        if not user_is_admin(request.user):
+            return Response(
+                {'detail': 'Seul un responsable peut supprimer un rapport.'},
+                status=status.HTTP_403_FORBIDDEN,
+            )
+        return super().destroy(request, *args, **kwargs)
+
 
 class LigneRapportViewSet(viewsets.ModelViewSet):
     queryset = LigneRapport.objects.all()
90 /home/daniel/FInal/carburflow/.tmp_local_diff.txt
